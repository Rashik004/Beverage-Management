@model IEnumerable<BeverageManagement.Models.ApplicationUser>

@{
    ViewBag.Title = "hh";
}

<h2>hh</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table bootstrap-table-convert" data-show-columns="true" data-search="true" data-pagination="true">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {

            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id }) |
                    @Html.ActionLink("Make Admin", "AddAsAdmin", new { id = item.Id }) |
                    @Html.ActionLink("Remove Admin", "RemoveFromAdmin", new { id = item.Id }) 


                </td>
            </tr>

        }
    </tbody>
</table>

